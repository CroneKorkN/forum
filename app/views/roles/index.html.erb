<h2>
  Roles
  <%= link_to '[+]', new_role_path %>
</h2>

<ul class=roles>
  <% @roles.each do |role| %>
    <li>
      <%= link_to role.name, edit_role_path(role) %>
      <ul>
        <%= render role.role_permissions %>
      </ul>
    </li>
  <% end %>
</ul>

