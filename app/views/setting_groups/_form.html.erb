<%= form_for(@setting_group) do |f| %>
  <% if @setting_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@setting_group.errors.count, "error") %> prohibited this setting_group from being saved:</h2>

      <ul>
      <% @setting_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :setting_group_id %><br>
    <%= f.text_field :setting_group_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
